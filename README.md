# PYTHON 3D Engine

## Made in the [Python Turtle](https://docs.python.org/3/library/turtle.html) library 

### Runs on this one simple calculation:

X = (0.5 * fov_val) * ((x + camera X) / (z + 0.1 * camera Z))
Y = (0.5 * fov_val) * ((y + camera Y) / (z + 0.1 * camera Z))
